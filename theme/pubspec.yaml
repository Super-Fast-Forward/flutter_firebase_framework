name: theme
description: FFFW Themes
version: 0.0.1
homepage:

environment:
  sdk: ">=2.17.1 <3.3.3"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.3.0
  cloud_firestore: ^4.0.1
  # firebase_auth_web: 5.1.3
  firebase_auth_web: 5.5.0
  # firebase_auth: 4.1.5
  auth:
    git: 
      url: https://github.com/Super-Fast-Forward/flutter_firebase_framework.git
      path: auth
  providers:
    git: 
      url: https://github.com/Super-Fast-Forward/flutter_firebase_framework.git
      path: providers

dependency_overrides:
  auth:
    path: ../auth
  providers:
    path: ../providers



dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
